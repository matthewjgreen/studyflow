{
  "name": "studyflow-push-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Background worker that sends Trackr due-date reminders as Web Push notifications, even when the app is closed.",
  "scripts": {
    "start": "node index.js",
    "vapid": "web-push generate-vapid-keys --json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "dotenv": "^16.4.5",
    "web-push": "^3.6.7",
    "ws": "^8.21.0"
  }
}
